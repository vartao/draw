# 泳道图生成规则

## 适用场景
用于展示跨角色、部门、系统之间的流程责任和交接。

## 输出规则
- 泳道表示责任主体，例如“客户”“客服”“技术支持”“主管”“系统”。
- 每个节点必须且只能属于一个泳道。
- 跨泳道交接必须通过边表达，并尽量标注交接内容。
- 泳道数量通常不超过 6 个。
- 每条泳道内的节点按时间顺序排列。

## 禁止事项
- 不要把阶段当成泳道，除非用户明确要求按阶段分泳道。
- 不要让节点缺少 lane。
- 不要用多个名称表达同一个角色。

## 输出结构
输出 `lanes`、带 `lane` 字段的 `nodes`，以及 `edges`。
